#include "ackermann.hpp"

#include "church.hpp"
#include "eval.hpp"

// реализуйте лямбда-терм функции Аккермана только через построение
// термов с использованием term/parser/church/eval.
//
// Функция должна вернуть терм, эквивалентный:
//   λm.λn. A(m, n)
// где m и n — числа Чёрча.
//
// Ожидаемая идея решения:
//   1. Построить step-функцию Ackermann как лямбда-терм.
//   2. Использовать make_Z_combinator() для рекурсии.
//   3. Вернуть готовую лямбда-функцию, не вычисляя её в uint64_t.
//
// Private-тесты будут сами применять ваш терм к church(m) и church(n),
// нормализовывать результат и сравнивать его с нужным числом Чёрча.
std::shared_ptr<Term> make_ackermann() {
    // A(0, n) = n + 1
    // A(m + 1, 0) = A(m, 1)
    // A(m + 1, n + 1) = A(m, A(m + 1, n))
    
}
